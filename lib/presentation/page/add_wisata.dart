import 'package:flutter/material.dart';

class AddWisata extends StatelessWidget {
  const AddWisata({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: ListView());
  }
}
