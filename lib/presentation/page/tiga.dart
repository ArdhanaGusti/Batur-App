import 'package:flutter/material.dart';

class Tiga extends StatelessWidget {
  const Tiga({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Tiga")),
    );
  }
}
